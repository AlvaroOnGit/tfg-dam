/**
 * Model for tokens
 * Responsible for accessing, updating, deleting and creating token related information on the database.
 */
import pool from '../../db/connection.js';

export class TokenModel {

    //static async saveRefreshToken(data) {
    //    const res = await pool.query(`
    //    INSERT INTO refresh_tokens (user_id, device_id, token, expires_at, revoked_at, user_agent)
    //
    //    `)
    //}
}